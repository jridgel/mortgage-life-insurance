# API base URL — leave empty to use Vite's proxy (local dev default)
# Set this in production if your API is hosted on a different domain
# e.g. VITE_API_URL=https://your-api.onrender.com
VITE_API_URL=
